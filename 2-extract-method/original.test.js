const { calc_bmi } = require('./original');

test('calculate BMI', () => {
  // Not really possible to test the original!
  /*
  // Test case 1: weight = 70kg, height = 170cm
  expect(calc_bmi(70, 170)).toBeCloseTo(24.22, 2);

  // Test case 2: weight = 80kg, height = 180cm
  expect(calc_bmi(80, 180)).toBeCloseTo(24.69, 2);

  // Test case 3: weight = 60kg, height = 160cm
  expect(calc_bmi(60, 160)).toBeCloseTo(23.44, 2);
  */
});
